* text=auto eol=lf
*.sh text eol=lf
gradlew text eol=lf
# Ensure gradlew always uses Unix line endings
gradlew text eol=lf

# Ensure Windows batch files use Windows line endings
*.bat text eol=crlf

# Ensure YAML files use Unix line endings
*.yml text eol=lf
*.yaml text eol=lf

# Auto-detect text files and perform LF normalization for other files
* text=auto
